what the script does:
-load data csv file
-data exploration(df.head(), df.shape ,df.describe(),df.dtypes,df.isnull().sum())
-data cleaning(rename columns, fill null values, change data type)
-summary statistics:(Row count , column stats , anomalies[missing values and duplicacte value])
-clean version to a new CSV output file


how to run it:
-enviroment setup
-run the python_task3.py


what the input and output files are:
input csv file : titanic(train_and_test2).csv
output files : csv_output_file