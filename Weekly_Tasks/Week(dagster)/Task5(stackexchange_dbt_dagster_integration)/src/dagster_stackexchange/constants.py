import os

# Absolute path to your existing dbt project
DBT_PROJECT_DIR = r"C:\datumlabs-launchpad\Weekly_Tasks\Week(dbt)\dbt_tasks\dbt_stack_exchange"

DBT_MANIFEST_PATH = os.path.join(DBT_PROJECT_DIR, "target", "manifest.json")