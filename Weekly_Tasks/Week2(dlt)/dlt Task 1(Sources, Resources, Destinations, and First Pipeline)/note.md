source: original place or system where raw data comes from.
resource: the specific target table ,file,or end point inside the source that you want to extract.
schema: the blueprint / structure of the data that defines columns and data types
destination: the warehouse where the processed data is stored
pipeline: the automated system that moves , clean and transfer data from source to destination
