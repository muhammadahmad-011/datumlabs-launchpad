1. A resource fetches its own data independently, while a transformer receives items from another resource via | ,(e.g., using each question's question_id to fetch its comments) 

2. use a transformer whenever child data depends on a value only the parent record provides.